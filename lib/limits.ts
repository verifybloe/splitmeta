/** Free users see this many sample races per unlocked setup on the board. */
export const FREE_BOARD_RACE_LIMIT = 3;

/** Free users can open full details on this many most-recent races. */
export const FREE_RACE_DETAIL_LIMIT = 3;

/** Max session uploads per user per rolling 24h (anti-poisoning). */
export const INGEST_DAILY_LIMIT = 40;
